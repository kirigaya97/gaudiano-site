---
// Option 6 — Fusion + Customer Modifications
// (Duplicate of Option 5 with further refinements)
// Palette: option 4 (alabaster / navy #1B263B / cashmere #D7C9B4)
// Typography: option 1 (Playfair Display + Lora)
// Color combos in type: option 3 style (cashmere accents on dark)
// Layout structure: option 1 (editorial, 2-col hero, sep-lines)
// Counters: option 3 (animated stats strip)
// Pilares circles: option 4 (rounded-full numbered orbs with hover)
// Protocol hover: option 3 (row indent + bg on hover)
// Sobre mí: added horizontal slide-in movement
// Resultados boxes: enhanced contrast on hover
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Oasis de Claridad | Romina Gaudiano"
  fonts="Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&family=Lora:ital,wght@0,400;0,500;1,400"
>
  <style is:global>
    :root {
      --navy:    #1B263B;
      --cream:   #F5F2EB;
      --charcoal:#2A2A2A;
      --cashm:   #D7C9B4;
    }

    /* Scrollbar — whisper-thin, cashmere thumb fading at ends */
    ::-webkit-scrollbar              { width: 2px; }
    ::-webkit-scrollbar-track        { background: rgba(27,38,59,0.4); }
    ::-webkit-scrollbar-thumb        {
      background: linear-gradient(
        to bottom,
        transparent 0%,
        var(--cashm) 15%,
        var(--cashm) 85%,
        transparent 100%
      );
      border-radius: 0;
    }
    ::-webkit-scrollbar-thumb:hover  { background: var(--cream); }
    * { scrollbar-width: thin; scrollbar-color: var(--cashm) transparent; }
    ::selection { background: var(--cashm); color: var(--navy); }

    /* Hide default cursor globally for this page */
    .o6-page, .o6-page * { cursor: none !important; }

    /* Custom cursor */
    #o6-cursor-dot {
      position: fixed; top: 0; left: 0; z-index: 9999;
      width: 6px; height: 6px; border-radius: 50%;
      background: var(--cashm);
      pointer-events: none;
      transform: translate(-50%, -50%);
      transition: opacity 0.3s;
    }
    #o6-cursor-ring {
      position: fixed; top: 0; left: 0; z-index: 9998;
      width: 36px; height: 36px; border-radius: 50%;
      border: 1px solid rgba(215,201,180,0.55);
      pointer-events: none;
      transform: translate(-50%, -50%);
      transition: width 0.3s, height 0.3s, border-color 0.3s, opacity 0.3s;
    }
    #o6-cursor-ring.hovered {
      width: 52px; height: 52px;
      border-color: var(--cashm);
    }

    /* CSS scroll-driven reveal */
    .o6-reveal {
      opacity: 0;
      transform: translateY(28px);
      animation: o6-reveal-up linear both;
      animation-timeline: view();
      animation-range: entry 0% entry 38%;
    }
    @keyframes o6-reveal-up {
      from { opacity: 0; transform: translateY(28px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    /* Sep-line expand */
    @keyframes o6-sep-grow {
      from { transform: scaleX(0); transform-origin: left; }
      to   { transform: scaleX(1); transform-origin: left; }
    }
    .o6-sep {
      animation: o6-sep-grow 1.4s cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
      animation-timeline: view();
      animation-range: entry 0% entry 30%;
    }

    /* Hero entrance */
    @keyframes o6-hero-enter {
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes o6-img-reveal {
      from { opacity: 0; clip-path: inset(0 100% 0 0); }
      to   { opacity: 1; clip-path: inset(0 0% 0 0); }
    }
    @keyframes o6-rule-expand {
      from { width: 0; opacity: 0; }
      to   { opacity: 1; }
    }
    .o6-hero-item {
      opacity: 0;
      transform: translateY(20px);
      animation: o6-hero-enter 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
    }
    .o6-hero-img {
      opacity: 0;
      animation: o6-img-reveal 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
      animation-delay: 0.1s;
    }
    .o6-hero-rule {
      opacity: 0;
      animation: o6-rule-expand 0.6s ease forwards;
      animation-delay: 1s;
    }

    /* Nav underline */
    .o6-nav-link { position: relative; }
    .o6-nav-link::after {
      content: ''; position: absolute; bottom: -2px; left: 0;
      width: 0; height: 1px; background: var(--cashm);
      transition: width 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    }
    .o6-nav-link:hover::after { width: 100%; }
    .o6-nav-link:hover { color: var(--cashm); }

    /* Hamburger Menu Overlay */
    #o6-menu-toggle:checked ~ #o6-menu-overlay { 
      opacity: 1; pointer-events: auto; 
    }
    #o6-menu-toggle:checked ~ #o6-menu-overlay .o6-menu-content {
      transform: translateY(0);
    }
    .o6-menu-link {
      font-family: 'Playfair Display', serif;
      transition: color 0.4s ease, transform 0.4s ease;
      display: block;
    }
    .o6-menu-link:hover {
      color: var(--cashm);
      transform: translateX(10px);
    }

    /* Dynamic Pilares Section Hover using :has() */
    #o6-pilares {
      transition: background 0.7s cubic-bezier(0.25, 0.46, 0.45, 0.94), color 0.7s ease;
      --section-bg: var(--charcoal);
      --section-text: var(--cashm);
      --pilar-circle-bg: var(--charcoal);
      --pilar-circle-text: var(--cream);
      --pilar-line-color: rgba(215, 201, 180, 0.1);
      background: var(--section-bg);
      color: var(--section-text);
    }

    #o6-pilares:has(.o6-pilar-card:nth-child(2):hover) {
      --section-bg: var(--navy);
      --section-text: var(--cream);
      --pilar-circle-bg: var(--navy);
      --pilar-circle-text: var(--cashm);
      --pilar-line-color: rgba(245, 242, 235, 0.07);
    }
    #o6-pilares:has(.o6-pilar-card:nth-child(3):hover) {
      --section-bg: var(--cashm);
      --section-text: var(--navy);
      --pilar-circle-bg: var(--navy);
      --pilar-circle-text: var(--cream);
      --pilar-line-color: rgba(27, 38, 59, 0.1);
    }
    #o6-pilares:has(.o6-pilar-card:nth-child(4):hover) {
      --section-bg: var(--cream);
      --section-text: var(--charcoal);
      --pilar-circle-bg: var(--navy);
      --pilar-circle-text: var(--cashm);
      --pilar-line-color: rgba(27, 38, 59, 0.08);
    }

    .o6-pilar-circle { background: var(--pilar-circle-bg) !important; color: var(--pilar-circle-text) !important; }
    #o6-pilares-line { background: var(--pilar-line-color) !important; }

    /* Fix contrasts */
    .o6-emphasis { color: var(--cream) !important; font-weight: 700; }
    .o6-contrast-heading { font-weight: 700; color: var(--navy) !important; }
    
    /* Stats */
    .o6-stat-num {
      font-family: 'Playfair Display', serif;
      font-weight: 700;
      font-size: clamp(3.5rem, 6vw, 5.5rem);
      line-height: 1;
      color: var(--navy);
    }

    /* Protocolo rows — option 3 hover */
    .o6-offer-row {
      transition: background 0.4s ease, padding-left 0.4s ease;
    }
    .o6-offer-row:hover {
      padding-left: 1.5rem;
      background: rgba(27, 38, 59, 0.04);
    }

    /* Pilares circles — option 4 orb hover */
    .o6-pilar-card { transition: transform 0.5s ease; cursor: default; }
    .o6-pilar-card:hover { transform: translateY(-6px); }
    .o6-pilar-circle {
      transition: background 0.5s ease, color 0.5s ease, transform 0.4s ease, box-shadow 0.4s ease;
    }
    .o6-pilar-card:hover .o6-pilar-circle {
      background: var(--cashm);
      color: var(--navy);
      transform: scale(1.1);
      box-shadow: 0 0 36px rgba(215, 201, 180, 0.28);
    }
    .o6-pilar-desc { transition: opacity 0.4s ease; }
    .o6-pilar-card:hover .o6-pilar-desc { opacity: 1; }

    /* Hero image zoom on hover */
    .o6-hero-img img {
      transition: transform 1.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    }
    .o6-hero-img:hover img { transform: scale(1.06); }

    /* Sobre mí — slide-in from sides */
    .o6-sobre-left {
      opacity: 0;
      transform: translateX(-40px);
      animation: o6-slide-left linear both;
      animation-timeline: view();
      animation-range: entry 0% entry 48%;
    }
    .o6-sobre-right {
      opacity: 0;
      transform: translateX(40px);
      animation: o6-slide-right linear both;
      animation-timeline: view();
      animation-range: entry 0% entry 48%;
    }
    @keyframes o6-slide-left {
      to { opacity: 1; transform: translateX(0); }
    }
    @keyframes o6-slide-right {
      to { opacity: 1; transform: translateX(0); }
    }

    /* Resultados boxes — contrast hover */
    .o6-resultado-box {
      border: 1px solid rgba(215, 201, 180, 0.18);
      transition: background 0.35s ease, border-color 0.35s ease, transform 0.35s ease, box-shadow 0.35s ease;
      cursor: default;
    }
    .o6-resultado-box:hover {
      background: rgba(215, 201, 180, 0.13);
      border-color: var(--cashm);
      transform: translateY(-5px);
      box-shadow: 0 8px 28px rgba(215, 201, 180, 0.12);
    }
    .o6-resultado-text { transition: color 0.35s ease; }
    .o6-resultado-box:hover .o6-resultado-text {
      color: var(--cashm);
      font-style: italic;
    }
    .o6-resultado-box .o6-resultado-line {
      transition: width 0.35s ease, opacity 0.35s ease;
    }
    .o6-resultado-box:hover .o6-resultado-line {
      width: 2rem;
      opacity: 0.9;
    }
  </style>

  <!-- Custom cursor elements (desktop only) -->
  <div id="o6-cursor-dot" class="hidden md:block"></div>
  <div id="o6-cursor-ring" class="hidden md:block"></div>

  <div class="o6-page overflow-x-hidden max-w-[100vw]" style="background: var(--navy); color: var(--cream); min-height: 100vh; font-family: 'Lora', serif;">

    <!-- Hamburger Toggle Input (Hidden) & Overlay -->
    <input type="checkbox" id="o6-menu-toggle" class="hidden peer" />
    <div id="o6-menu-overlay" class="fixed inset-0 w-full h-full bg-[#2A2A2A]/98 z-[120] opacity-0 pointer-events-none transition-opacity duration-500 flex items-center justify-center">
      <!-- Close Button (X) -->
      <label for="o6-menu-toggle" class="absolute top-8 left-8 md:top-12 md:left-12 text-cream/30 hover:text-cashm text-4xl md:text-5xl cursor-pointer transition-colors z-[130] font-light">
        ×
      </label>

      <div class="o6-menu-content transform -translate-y-10 transition-transform duration-700 flex flex-col items-center gap-8 md:gap-10 text-center px-6">
        <a href="/option-6"  class="o6-menu-link text-3xl md:text-4xl text-cream tracking-tight">Home</a>
        <a href="#mentoria"  class="o6-menu-link text-3xl md:text-4xl text-cream tracking-tight">Reprogramación mental para traders: ¿Por qué?</a>
        <a href="#protocolo" class="o6-menu-link text-3xl md:text-4xl text-cream tracking-tight">Formato de la Mentoría</a>
        <a href="#sobre-mi"  class="o6-menu-link text-3xl md:text-4xl text-cream tracking-tight">Sobre Mí</a>
        
        <label for="o6-menu-toggle" class="mt-8 text-[10px] tracking-[0.4em] uppercase text-cashm/50 cursor-pointer hover:text-cream transition-colors">
          Cerrar Menú
        </label>
      </div>
    </div>

    <!-- ── Nav ───────────────────────────────────────────── -->
    <nav class="fixed top-0 left-0 right-0 z-[100] flex justify-between items-center px-6 md:px-10 py-5 md:py-6 transition-shadow duration-500"
         style="background: rgba(27,38,59,0.93); backdrop-filter: blur(8px);">

      <div class="flex items-center gap-4">
        <!-- Hamburger Icon -->
        <label for="o6-menu-toggle" class="cursor-pointer group flex flex-col gap-1.5 z-[110] relative">
          <span class="w-6 h-px bg-cream transition-all group-hover:bg-cashm"></span>
          <span class="w-6 h-px bg-cream transition-all group-hover:bg-cashm"></span>
          <span class="w-4 h-px bg-cream transition-all group-hover:bg-cashm"></span>
        </label>
        <span class="text-xs tracking-[0.32em] uppercase"
              style="color: var(--cashm); font-family: 'Playfair Display', serif;">
          Romina Gaudiano
        </span>
      </div>

      <a href="#contacto"
         class="text-[10px] md:text-xs tracking-[0.2em] md:tracking-[0.25em] uppercase px-4 md:px-6 py-2 md:py-2.5 transition-all duration-300"
         style="background: var(--cashm); color: var(--navy);"
         onmouseover="this.style.background='var(--cream)'"
         onmouseout="this.style.background='var(--cashm)'">
        Aplicá Acá
      </a>
    </nav>

    <!-- ── Hero ──────────────────────────────────────────── -->
    <!--
      SECTION LAYOUT NOTES:
      - 'relative'        → required so the mobile portrait (absolute) is positioned relative to this section
      - 'overflow-hidden' → clips the mobile portrait that intentionally bleeds past the right edge
      - 'grid md:grid-cols-2' → 2-column grid on desktop, single stacked column on mobile
      - 'min-height: 100vh' → always fills the full viewport height
    -->
    <section class="relative grid md:grid-cols-2 overflow-hidden" style="min-height: 100vh;">

      <!--
        ═══════════════════════════════════════════════════════════
        DESKTOP PORTRAIT — Left column of the 2-col grid
        - 'hidden md:block' → only rendered on md+ screens (≥768px)
        - Gradient goes transparent → navy (left to right) to blend into the text column on the right
        - No horizontal mirror: she faces right, toward the text content
        ═══════════════════════════════════════════════════════════
      -->
      <div class="relative hidden md:block o6-hero-img overflow-hidden">
        <img src="/gaudiano1.webp" alt="Romina Gaudiano"
             class="absolute inset-0 w-full h-full object-cover object-top"
             style="filter: grayscale(25%) contrast(1.05);" />
        <!-- Gradient: covers the right edge of the image to blend into the text column -->
        <div class="absolute inset-0"
             style="background: linear-gradient(to right, transparent, var(--navy));"></div>
      </div>

      <!--
        ═══════════════════════════════════════════════════════════
        MOBILE PORTRAIT — Cinematic right-side overlay
        - 'absolute md:hidden' → only on mobile; removed from grid flow so text overlaps it
        - 'inset-y-0' → fills the full section height from top to bottom
        - 'z-0' → renders behind the content column (which is z-10)

        PROPORTIONS (adjust these to reframe the portrait):
        • width: 80vw     → total image div width. Increase to show more of her body.
        • right: -20vw    → shifts the div 20vw past the right viewport edge.
                            Result: 80vw - 20vw = 60vw visible (~3/4 on screen, 1/4 off-screen right).
                            Make more negative to bleed more off-screen.
        ═══════════════════════════════════════════════════════════
      -->
      <div class="absolute md:hidden inset-y-0 z-0"
           style="width: 80vw; right: -20vw;">

        <!--
          Portrait image:
          - 'transform: scaleX(-1)' → mirrors horizontally so she faces LEFT (toward the text)
          - 'object-top'            → anchors to the top (face/upper body stays in frame)
          - Same grayscale/contrast filter as the desktop version for visual consistency
          Note: alt="" intentional — this is decorative/duplicate, screen readers use the desktop version
        -->
        <img src="/gaudiano1.webp" alt=""
             class="absolute inset-0 w-full h-full object-cover object-top"
             style="filter: grayscale(25%) contrast(1.05); transform: scaleX(-1);" />

        <!--
          Left gradient overlay:
          - Starts at full navy (var(--navy)) on the LEFT edge of this div (~40vw from screen left)
          - Fades through a semi-transparent mid-stop at 55%
          - Reaches transparent at 80%, letting the portrait come through on the right

          HOW TO ADJUST:
          • Move "55%" left (e.g. 40%) → shows more of her face earlier
          • Move "55%" right (e.g. 70%) → stronger cover, less face visible through gradient
          • Change the mid rgba() alpha (0.4) → lower = more see-through in the middle
        -->
        <div class="absolute inset-0"
             style="background: linear-gradient(to right, var(--navy) 0%, rgba(27,38,59,0.7) 95%, transparent 80%);"></div>
      </div>

      <!--
        ═══════════════════════════════════════════════════════════
        CONTENT COLUMN — Text, headline, CTA
        - 'relative z-10' → ensures text renders ABOVE the mobile absolute portrait (z-0)
        - On mobile: full-width, content anchored to the bottom (justify-end)
        - On desktop: right column of the 2-col grid
        ═══════════════════════════════════════════════════════════
      -->
      <div class="flex flex-col justify-end pb-24 md:pb-36 px-6 md:px-16 pt-20 md:pt-24 relative z-10">
        <div class="max-w-4xl">
          <p class="o6-hero-item text-xs tracking-[0.4em] uppercase mb-10"
             style="color: var(--cashm); animation-delay: 0.35s;">
            Mentoría 1:1 – CryptoMind Pro Elite
          </p>
          <h1 class="o6-hero-item text-[1.6rem] leading-tight md:text-[3rem] lg:text-[4.8rem] md:leading-[1.05] mb-8 md:mb-12"
              style="font-family: 'Playfair Display', serif; animation-delay: 0.6s;">
            El poder de una mente clara<br />
            <em style="color: var(--cashm);">para invertir exitosamente.</em>
          </h1>
          <div class="o6-hero-rule w-16 h-px mb-12"
               style="background: rgba(245,242,235,0.18);"></div>
          <p class="o6-hero-item text-lg leading-relaxed max-w-md mb-4"
             style="color: rgba(245,242,235,0.7); animation-delay: 1.05s;">
            Reprogramación Mental y Cambio de Hábitos para Traders e Inversores.
          </p>
          <p class="o6-hero-item text-base leading-relaxed max-w-md mb-12"
             style="color: rgba(245,242,235,0.45); animation-delay: 1.1s;">
            Diseñada para quienes buscan optimizar su rendimiento interno, sostener claridad emocional y tomar decisiones conscientes bajo presión.
          </p>
          <p class="o6-hero-item text-sm md:text-base tracking-[0.25em] uppercase mb-10"
             style="color: var(--cashm); font-family: 'Playfair Display', serif; animation-delay: 1.18s;">
            Disciplina + Enfoque + Confianza = <em>Éxito</em>
          </p>
          <a href="#contacto"
             class="o6-hero-item inline-block text-xs tracking-[0.3em] uppercase px-10 py-4 transition-all duration-500"
             style="border: 1px solid rgba(245,242,235,0.2); animation-delay: 1.25s;"
             onmouseover="this.style.background='var(--cream)'; this.style.color='var(--navy)'"
             onmouseout="this.style.background='transparent'; this.style.color='var(--cream)'">
            Aplicá Acá
          </a>
        </div>
      </div>

    </section>


    <!-- ── New "Impacto" Section ──────────────────────────── -->
    <section class="relative py-16 md:py-32 px-6 md:px-20 o6-reveal overflow-hidden" style="background: var(--cashm); color: var(--navy);">

      <div class="relative z-10 max-w-6xl mx-auto grid md:grid-cols-[1fr_1.4fr] gap-12 md:gap-24 items-center">

        <!-- Left: Image (option 4 "El Desafío" overlay style — no frame) -->
        <div class="relative">
          <div class="overflow-hidden aspect-[4/3] relative shadow-xl">
            <img src="/impacto-trading.webp" alt="Mentalidad en el trading"
                 class="w-full h-full object-cover" />
            <div class="absolute inset-0 z-10 mix-blend-multiply pointer-events-none"
                 style="background: rgba(27,38,59,0.10);"></div>
          </div>
        </div>

        <!-- Right: Text -->
        <div>
          <h2 class="text-2xl md:text-4xl lg:text-5xl leading-tight" style="font-family: 'Playfair Display', serif;">
            ¿Te sentís como un auto <br class="hidden md:block"/>
            <em>de carreras sin frenos?</em>
          </h2>
          <h2 class="text-2xl md:text-4xl lg:text-5xl leading-tight mt-8" style="font-family: 'Playfair Display', serif;">
            ¿Cuál es el costo emocional <br class="hidden md:block"/>
            <em>de tu éxito financiero?</em>
          </h2>
          <p class="text-lg italic mt-10 md:mt-12" style="color: var(--navy); font-family: 'Lora', serif;">
            La verdadera libertad no está en el mercado, sino en la claridad mental con la que operás en él.
          </p>
        </div>

      </div>
    </section>

    <!-- Thin blue separator: 80% width centered, between impacto and counters -->
    <div style="background: var(--cashm); padding: 0;">
      <div class="mx-auto" style="width: 80%; height: 1px; background: var(--navy); opacity: 0.35;"></div>
    </div>


    <!-- ── Stats strip (option 3 animated counters) ───────── -->
    <div style="background: var(--cashm); padding: 3rem 1.5rem;">
      <div class="max-w-5xl mx-auto grid grid-cols-1 sm:grid-cols-3 gap-8 text-center">
        <div class="py-4 sm:py-0">
          <p class="o6-stat-num" data-count="4500" data-suffix="+">4500+</p>
          <p class="text-xs tracking-[0.3em] uppercase mt-3"
             style="color: rgba(27,38,59,0.6); font-family: 'Lora', serif;">Procesos de transformación</p>
        </div>
        <div class="py-4 sm:py-0"
             style="border-left: 1px solid rgba(27,38,59,0.15); border-right: 1px solid rgba(27,38,59,0.15);">
          <p class="o6-stat-num" data-count="12">12</p>
          <p class="text-xs tracking-[0.3em] uppercase mt-3"
             style="color: rgba(27,38,59,0.6); font-family: 'Lora', serif;">Sesiones personalizadas</p>
        </div>
        <div class="py-4 sm:py-0">
          <p class="o6-stat-num" data-count="3">3</p>
          <p class="text-xs tracking-[0.3em] uppercase mt-3"
             style="color: rgba(27,38,59,0.6); font-family: 'Lora', serif;">Meses de proceso</p>
        </div>
      </div>
    </div>

    <div class="o6-sep mx-4 md:mx-20"
         style="border-top: 1px solid rgba(245,242,235,0.08);"></div>

    <!-- ── ¿Por qué? ──────────────────────────────────────── -->
    <section id="mentoria" class="py-16 md:py-32 px-6 md:px-20 o6-reveal">
      <div class="max-w-6xl mx-auto grid md:grid-cols-[1.2fr_1.8fr] gap-12 md:gap-24 items-center">
        
        <div>
          <div class="relative group mx-2 md:mx-0">
            <div class="overflow-hidden aspect-[4/5] relative"
                 style="border-radius: 2%; box-shadow: 0 12px 40px rgba(0,0,0,0.35);">
              <img src="/trader-analysis.webp" alt="Análisis de trading profesional"
                   class="w-full h-full object-cover transition-transform duration-1000 group-hover:scale-105" />
            </div>
          </div>
        </div>

        <div>
          <h2 class="text-3xl md:text-5xl leading-tight mb-10"
              style="font-family: 'Playfair Display', serif;">
            ¿Por qué una mentoría de reprogramación mental <br />
            <em style="color: var(--cashm);">para traders e inversores?</em>
          </h2>
          <p class="leading-loose text-lg mb-8" style="color: rgba(245, 242, 235, 0.75);">
            Porque los hábitos que determinan tus decisiones económicas no son conscientes: <em style="color: var(--cashm); font-style: normal;">no se corrigen con fuerza de voluntad</em>, sino mediante la reestructuración de patrones neuronales automáticos.
          </p>
          <p class="leading-loose text-lg mb-8" style="color: rgba(245, 242, 235, 0.75);">
            En un entorno de alta incertidumbre, el cerebro activa mecanismos de supervivencia que nublan el juicio racional. Esta mentoría te permite <em style="color: var(--cashm);">intervenir en el proceso biológico</em> antes de que afecte tu capital.
          </p>
          <p class="leading-loose text-lg" style="color: rgba(245, 242, 235, 0.75);">
            No es casualidad que incluso traders experimentados repitan errores técnicos. El problema no es la estrategia, es la <em style="color: var(--cashm); font-style: normal;">respuesta interna frente al riesgo, la ganancia y la pérdida.</em>
          </p>
        </div>
      </div>
    </section>

    <div class="o6-sep mx-4 md:mx-20"
         style="border-top: 1px solid rgba(245,242,235,0.08);"></div>


    <!-- ── Pilares (Dynamic Hover Section) ── -->
    <section id="o6-pilares" class="py-16 md:py-32 px-6 md:px-20 overflow-hidden">
      <div class="max-w-5xl mx-auto">

        <div class="text-center mb-16 o6-reveal">
          <p class="text-xs tracking-[0.4em] uppercase mb-4" style="color: var(--cashm);">Cómo se trabaja</p>
          <h2 style="font-family: 'Playfair Display', serif; line-height: 1; display: flex; align-items: baseline; justify-content: center; gap: 0.3em;">
            <span style="font-size: clamp(3rem, 8vw, 7rem); font-weight: 700; line-height: 1;">3</span>
            <em style="font-size: clamp(2rem, 4vw, 3.5rem); opacity: 0.6;">Pilares.</em>
          </h2>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-12 md:gap-16 relative">
          <div id="o6-pilares-line" class="hidden md:block absolute top-12 left-[8%] right-[8%] h-px"></div>

          {[
            {
              step: "01",
              title: "Reprogramación Mental Subconsciente",
              desc: "No analizamos el síntoma. Reprogramamos el patrón que lo genera y lo cambiamos de raíz."
            },
            {
              step: "02",
              title: "Neurociencias Aplicadas a la Regulación Emocional",
              desc: "Prácticas para dominar la biología del enfoque con el objetivo de lograr estabilidad emocional bajo presión."
            },
            {
              step: "03",
              title: "Cambio de Hábitos de Alto Rendimiento",
              desc: "Sincronizamos la arquitectura mental con la emocional para consolidar una disciplina de alto impacto."
            }
          ].map(item => (
            <div class="o6-pilar-card o6-reveal flex flex-col items-center text-center transition-all duration-300">
              <div class="o6-pilar-circle w-24 h-24 rounded-full border flex items-center justify-center mb-8 transition-all duration-500"
                   style="border-color: rgba(215,201,180,0.22); font-family: 'Playfair Display', serif; font-size: 1.5rem;">
                {item.step}
              </div>
              <h3 class="text-xl md:text-2xl mb-4 leading-snug px-2"
                  style="font-family: 'Playfair Display', serif;">
                {item.title}
              </h3>
              <p class="o6-pilar-desc text-sm leading-loose max-w-xs opacity-70">
                {item.desc}
              </p>
            </div>
          ))}
        </div>

      </div>
    </section>

    <!-- ── Metodología (option 4 layout: Formato list + Resultados card) ── -->
    <section id="protocolo" class="py-16 md:py-32 px-6 md:px-20"
             style="background: var(--cream); color: var(--charcoal);">
      <div class="max-w-7xl mx-auto">

        <div class="grid grid-cols-1 lg:grid-cols-12 gap-16 lg:gap-24 items-start">

          <!-- LEFT: Formato numbered list -->
          <div class="lg:col-span-7 flex flex-col gap-12">
            <div class="o6-reveal">
              <p class="text-xs tracking-[0.4em] uppercase mb-4 o6-contrast-heading">Metodología</p>
              <h2 class="text-4xl md:text-5xl leading-tight mb-10"
                  style="font-family: 'Playfair Display', serif; color: var(--navy); font-weight: 700;">
                Formato de la <em style="color: var(--cashm);">Mentoría.</em>
              </h2>

              <div class="flex flex-col gap-6">

                <!-- 01 -->
                <div class="flex items-start gap-6 pb-6 border-b" style="border-color: rgba(27,38,59,0.1);">
                  <span class="text-xl italic mt-1"
                        style="font-family: 'Playfair Display', serif; color: var(--cashm);">01</span>
                  <div>
                    <h4 class="text-xl mb-2"
                        style="font-family: 'Playfair Display', serif; color: var(--navy); font-weight: 600;">
                      Modalidad: 1 a 1
                    </h4>
                    <ul class="space-y-2 text-lg" style="color: rgba(42,42,42,0.7); font-family: 'Lora', serif;">
                      <li>• 3 meses de duración total</li>
                      <li>• 12 sesiones individuales y personalizadas</li>
                      <li>• 1.30hs cada sesión semanal</li>
                    </ul>
                  </div>
                </div>

                <!-- 02 -->
                <div class="flex items-start gap-6 pb-6 border-b" style="border-color: rgba(27,38,59,0.1);">
                  <span class="text-xl italic mt-1"
                        style="font-family: 'Playfair Display', serif; color: var(--cashm);">02</span>
                  <div>
                    <h4 class="text-xl mb-2"
                        style="font-family: 'Playfair Display', serif; color: var(--navy); font-weight: 600;">
                      Acompañamiento entre sesiones
                    </h4>
                    <ul class="space-y-2 text-lg mb-4" style="color: rgba(42,42,42,0.7); font-family: 'Lora', serif;">
                      <li>• Seguimiento entre encuentros</li>
                      <li>• Acompañamiento en momentos críticos</li>
                      <li>• Ajustes personalizados según tu realidad operativa</li>
                    </ul>
                    <p class="text-sm italic" style="color: rgba(27,38,59,0.8); font-family: 'Lora', serif;">
                      "Acompañamiento real + proceso sostenido + valor entre sesiones."
                    </p>
                  </div>
                </div>

                <!-- 03 -->
                <div class="flex items-start gap-6">
                  <span class="text-xl italic mt-1"
                        style="font-family: 'Playfair Display', serif; color: var(--cashm);">03</span>
                  <div>
                    <h4 class="text-xl mb-2"
                        style="font-family: 'Playfair Display', serif; color: var(--navy); font-weight: 600;">
                      Utilización de IMOS<sup style="font-size: 0.55em; vertical-align: super;">™</sup>
                    </h4>
                    <p class="text-lg" style="color: rgba(42,42,42,0.7); font-family: 'Lora', serif;">
                      El primer sistema estructurado de integración y reconfiguración mental para entornos de riesgo e incertidumbre.
                    </p>
                  </div>
                </div>

              </div>
            </div>
          </div>

          <!-- RIGHT: Resultados card -->
          <div class="o6-reveal lg:col-span-5">
            <div class="p-10 md:p-14 relative"
                 style="background: var(--cashm); box-shadow: 0 18px 48px rgba(27,38,59,0.10);">
              <h3 class="text-3xl mb-8 leading-tight"
                  style="font-family: 'Playfair Display', serif; color: var(--navy); font-weight: 700;">
                Resultados que buscan<br />quienes eligen esta mentoría
              </h3>

              <ul class="space-y-5">
                {[
                  "Mayor estabilidad emocional",
                  "Decisiones más conscientes",
                  "Hábitos mentales sólidos",
                  "Menor reactividad",
                  "Protección del capital",
                  "Confianza interna sostenida"
                ].map(r => (
                  <li class="flex items-start gap-4">
                    <div class="mt-1 w-5 h-5 shrink-0 rounded-full flex items-center justify-center"
                         style="background: var(--navy);">
                      <svg class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="var(--cashm)">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7" />
                      </svg>
                    </div>
                    <span class="text-lg" style="color: var(--charcoal); font-family: 'Lora', serif;">{r}</span>
                  </li>
                ))}
              </ul>

              <div class="mt-12 pt-8 border-t" style="border-color: rgba(27,38,59,0.15);">
                <a href="#contacto"
                   class="block w-full text-center text-xs tracking-[0.3em] uppercase px-8 py-4 transition-all duration-500"
                   style="background: var(--navy); color: var(--cream);"
                   onmouseover="this.style.background='var(--charcoal)'"
                   onmouseout="this.style.background='var(--navy)'">
                  Aplicá Acá
                </a>
              </div>
            </div>
          </div>

        </div>

      </div>
    </section>

    <div class="o6-sep mx-4 md:mx-20"
         style="border-top: 1px solid rgba(245,242,235,0.08);"></div>

    <!-- ── Sobre Mí ── -->
    <section id="sobre-mi" class="relative overflow-hidden" style="background: var(--cashm); color: var(--charcoal);">

      <div class="relative z-10 max-w-6xl mx-auto px-6 md:px-20 py-20 md:py-32 grid md:grid-cols-2 gap-12 md:gap-20 items-center">

        <!-- Left: Bio -->
        <div class="o6-sobre-left">
          <p class="text-xs tracking-[0.4em] uppercase mb-8 o6-contrast-heading">Sobre Mí</p>
          <h2 class="text-4xl md:text-5xl lg:text-6xl leading-tight mb-10"
              style="font-family: 'Playfair Display', serif; color: var(--navy); font-weight: 700;">
            Romina <em style="color: var(--navy); font-weight: 700; font-style: normal;">Gaudiano.</em>
          </h2>

          <p class="leading-loose text-lg mb-8" style="color: rgba(42,42,42,0.85);">
            Diplomada en Neurociencias, Neurocoach y especialista en reprogramación de la mente subconsciente, con más de 4500 procesos de transformación real.
          </p>

          <blockquote class="leading-loose italic text-xl md:text-2xl md:pl-6"
                      style="color: var(--navy); font-family: 'Playfair Display', serif; border-left: 2px solid var(--navy); padding-left: 1.5rem;">
            "Trabajo con personas que entienden que el verdadero activo no es el mercado, sino la mente que decide dentro de él."
          </blockquote>
        </div>

        <!-- Right: Inserted portrait (not full bleed) -->
        <div class="o6-sobre-right flex justify-center md:justify-end">
          <div class="relative w-full max-w-sm md:max-w-md aspect-[4/5] overflow-hidden"
               style="border-radius: 2%; box-shadow: 0 14px 42px rgba(27,38,59,0.22);">
            <img src="/gaudiano2.webp" alt="Romina Gaudiano"
                 class="absolute inset-0 w-full h-full object-cover object-top" />
          </div>
        </div>

      </div>
    </section>


    <div class="o6-sep mx-4 md:mx-20"
         style="border-top: 1px solid rgba(245,242,235,0.08);"></div>

    <!-- ── CTA Final (Background Image + Text Refinement) ────── -->
    <section id="contacto" class="relative py-24 md:py-48 px-6 md:px-20 text-center overflow-hidden">
      <!-- Background Image with Overlay (Directly related to traders) -->
      <div class="absolute inset-0 z-0 scale-[1.05]">
        <img src="/trading_graphs_bg.webp" alt="" class="w-full h-full object-cover blur-[2px]" />
        <div class="absolute inset-0 bg-[#2A2A2A]/80 backdrop-blur-[2px]"></div>
      </div>

      <div class="relative z-10 max-w-4xl mx-auto o6-reveal">
        <p class="text-xs tracking-[0.4em] uppercase mb-6" style="color: var(--cashm);">
          Mentoría 1:1 · Cupos limitados
        </p>
        <h2 class="text-4xl md:text-5xl lg:text-7xl leading-[1.1] mb-10"
            style="font-family: 'Playfair Display', serif;">
          Mayor estabilidad.<br />
          <em style="color: var(--cashm);">Decisiones limpias.</em>
        </h2>
        <p class="max-w-lg mx-auto mb-10 text-lg leading-relaxed" style="color: rgba(245,242,235,0.65);">
          Hábitos mentales sólidos para proteger tu capital. <strong>Contactate</strong> ahora para reservar tu lugar.
        </p>
        <p class="text-sm md:text-base tracking-[0.3em] uppercase mb-8"
           style="color: var(--cashm); font-family: 'Playfair Display', serif;">
          Cupos limitados — <em>reservá ya tu lugar</em>
        </p>
        <a href="mailto:hola@rominagaudiano.com"
           class="inline-block text-xs tracking-[0.3em] uppercase px-10 md:px-14 py-5 transition-all duration-500 shadow-2xl"
           style="background: var(--cashm); color: var(--navy);"
           onmouseover="this.style.background='var(--cream)'; this.style.color='var(--navy)'"
           onmouseout="this.style.background='var(--cashm)'; this.style.color='var(--navy)'">
          Aplicá Acá
        </a>
      </div>
    </section>

    <!-- ── Footer ─────────────────────────────────────────── -->
    <footer class="px-6 md:px-20 py-10 md:py-16 flex flex-col md:flex-row justify-between items-center gap-8"
            style="background: var(--charcoal); border-top: 1px solid rgba(215,201,180,0.1);">
      
      <div class="flex flex-col items-center md:items-start gap-4">
        <span class="text-xs tracking-[0.3em] uppercase"
              style="color: rgba(245,242,235,0.4); font-family: 'Playfair Display', serif;">
          Romina Gaudiano
        </span>
        <span class="text-[10px] tracking-widest uppercase opacity-20 text-center md:text-left">Todos los derechos reservados &reg; Romina Gaudiano - {new Date().getFullYear()}</span>
      </div>

      <div class="flex flex-col items-center gap-3">
        <span class="text-[9px] tracking-[0.3em] uppercase opacity-30">Desarrollado por</span>
        <a href="https://www.instagram.com/numa.digital/" target="_blank" rel="noopener noreferrer" class="h-6 opacity-40 hover:opacity-80 transition-opacity duration-500">
          <svg id="Layer_1" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1080 208.66" class="h-full w-auto" style="fill: var(--cream); opacity: 0.7;">
            <path d="M501.3,164.68c-.17.03-1.26.21-1.99.33-17.94,2.97-16.12-19.43-16.29-19.18s.63-116.2.63-116.2l-31.17-.11-.11,62.38s.95,27.12-2.65,46.27-32.61,31.67-57.83,26.93c-25.22-4.74-26.52-23.21-27.44-33.12s-.92-102.48-.92-102.48h-31.25s.99,100.41,0,104.55-3.6,9.34-12.95,16.16c-9.34,6.81-11.77.63-11.95-2.47s2.73-41.25,4.55-57.63c1.82-16.38,2.43-23.05,0-32.46-2.43-9.4-4.55-17.59-15.77-26.39-11.22-8.8-23.91-8.55-34.28-3.03s-14.97,12.14-28.51,30.94-39.88,75.84-41.28,78.36-2.33.47-2.33.47c0,0,.43-78.2,0-89.74s-7.01-23.37-20.46-23.93-20.77,13.65-20.77,13.65l-26.3,50.02,22.16,13.75s11.75-19.54,14.39-24.81c2.64-5.28,2.64-1.48,2.64-1.48,0,0-1.35,83.91,2.47,99.6,3.83,15.69,15.78,16.46,20.57,15.69,4.78-.78,13.71-4.19,22.59-20.9,4.7-8.85,22.39-42.11,22.39-42.11,0,0,31.42-66.41,42.54-67.61s11.35,16.61,10.39,23.91-9.27,68.79-8.06,83.41c1.22,14.62,9.17,25.88,29.57,22.23,20.4-3.65,30.41-50.55,31.02-43.24.61,7.31,5.99,16.51,13.09,25.58,7.11,9.07,22.1,17.09,34.8,18.08,12.71.99,27.69,1.84,43.57-5.28s29.21-32.84,29.21-32.84c0,0,.99,18.81,4.29,25.25s6.93,10.73,23.76,11.22c16.83.5,17.81-1.89,17.81-3.71v-19.24c0-.27.02-.82-.15-.8Z"/>
            <path d="M503.04,29.03l.88,157.51h31.5s-.43-82.14.04-93.51c.47-11.37,3.5-32.56,23.8-41.69,20.29-9.13,39.85-1.38,45.2,5.44,5.35,6.83,11.25,10.15,11.44,33.94.18,23.8.07,95.81.07,95.81h30.37s-.83-85.69,1-96.48c1.83-10.79,5.15-28.87,27.51-39.82s40.16,4.03,45.73,11.81c5.57,7.78,6.92,12.82,7.09,24.47s-.22,100.02-.22,100.02h31.61s0-95.97,0-107.45-3.88-29.58-16.03-39.82c-12.15-10.24-25.65-15.7-39.15-15.7s-31.56,2.37-43.71,14.69c-12.15,12.32-16.71,22.78-16.71,22.78,0,0-.34,2.53-3.04-4.05s-9.45-22.95-30.37-30.54c-20.92-7.59-45.62,1.94-49.15,3.49s-19.65,13.36-25.18,26.17c-5.52,12.81-5.42-2.33-5.42-2.33v-24.74h-27.25Z"/>
            <path d="M926.68,164.74c-8.73,1.72-10.34,1.27-13.47-.9-5.17-3.58-5.06-10.53-4.98-12.31.12-2.86.8-46.98-1.07-75.09s-11.94-34.25-21.21-40.43c-9.26-6.18-59.37-19.53-88.29-4.81-28.91,14.72-29.84,41.35-29.84,41.35,0,0,27.15,3.16,27.92,3.94-.07-.68.74-7.82,3.95-13.55,2.72-4.85,7.41-7.86,7.41-7.86,0,0,23.18-18.28,50.27-6.7,27.09,11.58,22.26,32.2,10.05,40.09s-51.8,9.65-70.71,13.9c-18.91,4.25-30.27,17.02-33.47,33.85s5.1,33.01,11.52,39.68c11.39,12.94,31.6,14.98,45.99,14.98s25.96-2.75,37.52-10.09,20.69-25.67,20.69-25.67c0,0,1.5-.94,1.69,2.26.19,3.2,1.32,13.92,4.8,19.94,3.48,6.02,7.14,9.88,19.09,11.1s22.87-1.86,22.87-1.86l-.71-21.8ZM873.45,136.91c-1.5,6.12-2.62,26.04-32.72,30.95-30.1,4.91-48.37-2.28-49.3-19.09-.93-16.81,6.75-25.02,16.41-29.12s37.96-8.29,48.32-10.85c10.36-2.56,18.86-6.43,18.86-6.43,0,0-.08,28.43-1.58,34.55Z"/>
            <circle cx="948.07" cy="168.45" r="19.23"/>
          </svg>
        </a>
      </div>
    </footer>

  </div>

  <script>
    // ── Custom cursor (desktop only) ────────────────────────
    if (window.matchMedia('(hover: hover)').matches) {
      const dot  = document.getElementById('o6-cursor-dot')  as HTMLElement;
      const ring = document.getElementById('o6-cursor-ring') as HTMLElement;

      if (dot && ring) {
        // Dot and Ring follow mouse instantly (no lag)
        document.addEventListener('mousemove', e => {
          const mx = e.clientX; 
          const my = e.clientY;
          
          dot.style.left = mx + 'px';
          dot.style.top  = my + 'px';
          
          ring.style.left = mx + 'px';
          ring.style.top  = my + 'px';
        }, { passive: true });

        // Hover state on interactables
        document.querySelectorAll('a, button, label').forEach(el => {
          el.addEventListener('mouseenter', () => ring.classList.add('hovered'));
          el.addEventListener('mouseleave', () => ring.classList.remove('hovered'));
        });
      }
    }

    // ── Menu auto-close on link click ───────────────────────
    const menuToggle = document.getElementById('o6-menu-toggle') as HTMLInputElement;
    const menuLinks  = document.querySelectorAll('.o6-menu-link');
    menuLinks.forEach(link => {
      link.addEventListener('click', () => {
        if (menuToggle) menuToggle.checked = false;
      });
    });

    // ── Counter animation (option 3) ────────────────────────
    const counters = document.querySelectorAll('[data-count]');
    const counterObserver = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (!entry.isIntersecting) return;
        const el = entry.target as HTMLElement;
        const target = parseInt(el.dataset.count ?? '0');
        const suffix = el.dataset.suffix ?? '';
        const duration = 1600;
        let startTime: number | null = null;
        const tick = (timestamp: number) => {
          if (!startTime) startTime = timestamp;
          const progress = Math.min((timestamp - startTime) / duration, 1);
          const eased = 1 - Math.pow(1 - progress, 3);
          el.textContent = Math.floor(eased * target) + suffix;
          if (progress < 1) requestAnimationFrame(tick);
        };
        requestAnimationFrame(tick);
        counterObserver.unobserve(el);
      });
    }, { threshold: 0.6 });
    counters.forEach(c => counterObserver.observe(c));
  </script>

</BaseLayout>
